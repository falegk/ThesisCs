<% # flash_class(choice) is in the file application_helper.rb
   flash.each do |key, value| %>
    <div class="<%= flash_class(key) %> fade in">
      <button class="close" data-dismiss="alert" type="button" >×</button>
      <%= value %>
    </div>
<% end %>